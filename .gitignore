.venv/
backend/.venv/
**/__pycache__/
*.pyc
.pytest_cache/

# Node / frontend build output
node_modules/
frontend/dist/

# OS / editor cruft
.DS_Store
.vscode/

# Local-only secrets
.env
.env.local

# Signup accounts persisted at runtime — don't commit dev/test accounts.
# Remove this line if you want them versioned.
backend/bk_users_store.json

# Local SQLite database (used when DATABASE_URL is unset; Postgres in prod)
backend/*.db
